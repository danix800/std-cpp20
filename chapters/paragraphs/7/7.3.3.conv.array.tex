% 7.3.3 Array-to-pointer conversion [conv.array]
\paragraph{} % 1
一个类型为“\tm{N T}的数组”或“\tm{T}的未知上界的数组”的\lvalue{}或\rvalue{}可以转
换成类型为“\tm{T}的指针”的一个\prvalue{}。应用临时\mat{}转换（\ref{conv.rval}）。
结果为指向数组第一个元素的指针。
